# Replace values before first start (use scripts/bootstrap-local-lab.sh to automate).
basic_auth_users:
  alert_admin: $2y$12$replace_with_bcrypt_hash

tls_server_config:
  cert_file: /etc/alertmanager/certs/tls.crt
  key_file: /etc/alertmanager/certs/tls.key
  min_version: TLS12
