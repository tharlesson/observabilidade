# Alertmanager web configuration for TLS and basic auth.
basic_auth_users:
  alert_admin: ${ALERTMANAGER_BASIC_AUTH_HASH}

tls_server_config:
  cert_file: /etc/alertmanager/certs/tls.crt
  key_file: /etc/alertmanager/certs/tls.key
  min_version: TLS12
